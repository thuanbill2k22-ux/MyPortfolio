[build]
  base = "my portfolio/"
  # Thêm cờ --legacy-peer-deps vào đây
  command = "npm install --legacy-peer-deps && npm run build"
  publish = "dist/"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200